function fixTheMeerkat(arr) {
    return arr.reverse();/*The reverse() method reverses the order of the array elements in place. 
    The first element of the array becomes the last, and the last one becomes the first.*/
   }